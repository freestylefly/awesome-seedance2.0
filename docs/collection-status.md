# Collection Status

## 2026-05-14

- 已通过 Chrome 登录态打开飞书文档。
- 已滚动采集可见案例表格，并整理出首批结构化 case。
- 已建立本地媒体目录：`media/inputs/`、`media/outputs/`。
- 原始媒体文件仍需飞书下载或导出权限补齐。

## 下一步

1. 从飞书导出或逐个下载原始参考图片、参考视频、输出视频。
2. 按 `data/cases.json` 中的 `localPath` 放入媒体文件。
3. 将已补齐文件的 `status` 改成 `downloaded`。
4. 重新检查页面中视频播放和素材预览。
